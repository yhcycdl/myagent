# 多模态客服智能体比赛执行说明

> 这份文档用于发给 Codex，帮助其快速理解比赛目标、约束、评分重点，以及推荐的实现路线。内容基于目前已知的比赛材料、接口要求、样例题、评分规则，以及现阶段项目规划。

---

## 1. 比赛目标概述

本赛题要求构建一个**多模态客服智能体**，核心能力包括：

1. **多模态理解**
   - 能理解用户输入的文本问题。
   - 能理解用户上传的图片。
   - 能结合图文判断用户真实意图。

2. **RAG 知识增强**
   - 基于主办方提供的手册、说明书和配图构建知识库。
   - 针对问题返回最相关的手册内容。
   - 必要时同时返回相关配图，做到图文互补。

3. **多轮对话**
   - 支持 session_id。
   - 能处理用户追问、指代、省略表达。
   - 能针对一次提问中的多个子问题，拆分后逐条回答。

4. **幻觉抑制**
   - 回答尽量基于手册检索结果。
   - 对检索不到、证据不足的问题，不胡编乱造。
   - 优先输出“基于手册内容”的稳妥答案。

5. **工程交付**
   - 提供标准 RESTful API。
   - 提供源码、技术文档、验证报告。
   - 评委会通过 API 和材料综合打分。

---

## 2. 赛程信息

### 初赛

- 报名组队：2026-03-16 ~ 2026-06-20 24:00
- 初赛结果提交：2026-03-23 ~ 2026-06-20 24:00
- A 榜开放：2026-03-23
- B 榜开放：2026-06-20 09:00
- 初赛评审：2026-06-21 ~ 2026-07-05
- 决赛名单公布：2026-07-06

### 决赛

- 决赛线上准备与优化：2026-07-07 ~ 2026 年 8 月中下旬
- 线下决赛：2026 年 8 月中下旬

---

## 3. 评分规则与重点

### 初赛评分

#### 1）系统设计（30%）
主要看：
- 架构是否清晰
- 多模态理解 / RAG / 对话管理等模块是否划分明确
- 文档是否规范
- 接口定义是否完整
- 源码是否结构化、可复现
- 技术文档、验证报告是否完整

#### 2）技术实现（70%）
由自动评分系统对提交答案打分。

单题 1~5 分：
- **1 分**：没回应问题，结构乱，图片无关
- **2 分**：部分回应，不完整，图文结合较差
- **3 分**：基本回应，但缺深度，结构一般
- **4 分**：回答清晰全面，结构合理，图片有帮助
- **5 分**：详细有深度，结构严谨，图文完美互补

全部问题汇总后映射为 `[0.0, 1.0]` 技术实现分。

### 决赛评分

- 技术实现：70%
  - 初赛技术实现：40%
  - 决赛技术实现：30%
- 现场演示与答辩：30%

---

## 4. 输出要求

### 初赛必须提交 4 类材料

1. **智能体 API 接口**
   - 标准 RESTful API
   - 核心端点：`/chat`
   - 支持 JSON 文本输入
   - 支持 Base64 图片输入

2. **源码**
   - 包含多模态理解、RAG、多轮对话、幻觉抑制等模块
   - 可在隔离环境部署复现
   - 注释覆盖率不低于 30%

3. **技术文档（PDF）**
   - 系统架构设计
   - 核心技术方案
   - 创新点
   - 架构图 / 流程图

4. **验证报告**
   - 基于题目数据集的实验验证
   - 包括检索准确率、多模态理解效果、对话连贯性等
   - 包括截图、表格、结果分析

### 决赛需提交

- 优化报告
- 演示 PPT / PDF
- 优化后的 API、源码、技术文档和验证报告

---

## 5. 核心接口要求

### 基础要求

- 接口类型：RESTful API
- 核心端点：`POST /chat`
- 通信协议：HTTP/1.1（测试）、HTTPS（生产）
- 编码：UTF-8
- 接口超时：文本 20s、多模态 30s
- 认证方式：Bearer Token

### 请求头

- `Content-Type: application/json`
- `Authorization: Bearer {KAFU_API_TOKEN}`
- 可选：`X-Request-Id`
- 可选：`X-Client-Type`

### 请求体

```json
{
  "question": "我想更换健身追踪器的表带，有其他尺寸可选吗？",
  "images": ["data:image/png;base64,..."],
  "session_id": "kf_session_889900",
  "stream": false
}
```

### 响应体

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "answer": "您好，......",
    "session_id": "kf_session_889900",
    "timestamp": 1741008000
  }
}
```

### 实现注意事项

1. `question` 是核心必填字段，不能为空。
2. `images` 支持 0~3 张，每张 ≤ 5MB。
3. `session_id` 用于多轮对话。
4. 默认非流式即可，先把同步版本做稳。
5. API 必须稳定，评委会直接调用验证。

---

## 6. 从样例题中抽出的关键规律

### 规律 1：很多题是“一个问题里包含多个子问题”
例如：
- 能不能送到乡镇？
- 要不要额外运费？
- 多久能到？

所以系统必须支持：
- 问题拆分
- 分点回答
- 汇总成自然客服答复

### 规律 2：图片不是装饰，而是高分要素
高分答案通常要做到：
- 文本答对
- 返回的图也对
- 图能帮助用户理解

### 规律 3：手册问答必须强依赖检索
不能只把问题直接发给模型裸答。
必须做：
- 文本检索
- 图片关联
- 基于证据生成

### 规律 4：客服话术要稳
即使是手册类问题，也不建议回答得过于“论文风”或“百科风”。
风格应偏：
- 清楚
- 有条理
- 用户可执行
- 必要时给提醒

---

## 7. 总体技术路线建议

推荐路线：

**多模态理解 + 混合检索 RAG + 证据约束生成 + 多轮记忆 + 简单守护机制**

### 不推荐一开始做的事

- 一上来就微调
- 一上来就做复杂多 Agent
- 一上来就做花哨前端
- 只靠单个大模型裸答

### 推荐先做的事

1. 数据清洗与结构化
2. 文本 + 图片联合知识库
3. baseline 检索问答
4. `/chat` API 跑通
5. 评测脚本
6. 然后再做优化

---

## 8. 推荐系统架构

```text
用户问题 + 图片 + session_id
        │
        ▼
[输入预处理]
- 文本清洗
- Base64 图片解码
- 会话读取
        │
        ▼
[多模态理解]
- 产品识别
- 按钮/部件识别
- 意图识别
- 子问题拆分
- query 改写
        │
        ▼
[检索层]
- 向量检索
- BM25 关键词检索
- 图片关联召回
        │
        ▼
[重排层]
- reranker 排序
- 证据筛选
        │
        ▼
[生成层]
- 严格基于证据回答
- 多子问题逐条回答
- 客服风格组织
        │
        ▼
[守护与后处理]
- 幻觉检查
- 图片筛选
- 输出格式标准化
        │
        ▼
返回 answer / session_id / timestamp
```

---

## 9. 知识库设计要求

### 目标
不是做一个简单向量库，而是做一个：

**文本 chunk 与图片 ID 强绑定的多模态知识库**

### 推荐数据结构

#### 文本块示例

```json
{
  "chunk_id": "drill_chunk_001",
  "manual_name": "电钻手册",
  "product": "drill",
  "section_title": "DCB107 / DCB112 充电器指示灯",
  "page": 14,
  "text": "DCB107、DCB112 电池组充电中...电池组已充满...过热/过冷延迟...",
  "image_ids": ["drill10_04", "drill10_05", "drill10_06"],
  "keywords": ["DCB107", "DCB112", "指示灯", "闪烁"]
}
```

#### 图片块示例

```json
{
  "image_id": "drill10_04",
  "manual_name": "电钻手册",
  "product": "drill",
  "caption": "电池组充电中",
  "page": 14,
  "related_chunk_ids": ["drill_chunk_001"]
}
```

### 必须做的事

1. 保留章节标题
2. 保留产品名 / 型号
3. 保留页码
4. 把 `<PIC>` 与图片 ID 对齐
5. 每个 chunk 绑定对应图片
6. 中英文混合文本保留原貌

---

## 10. 检索方案建议

### 推荐采用混合检索

#### 1）向量检索
适合语义近似问题。

#### 2）BM25 / 关键词检索
适合：
- 型号
- 错误码
- 按钮名
- 专有名词
- 章节标题

#### 3）融合召回
例如：
- 向量 top 5
- BM25 top 5
- 合并去重后进入重排

#### 4）reranker 重排
用于提高最终证据质量。

### 推荐原则

- 宁可先召回更多候选，再精排
- 不要只靠单一路检索
- 型号、按钮、部件名称要高度重视

---

## 11. 多模态理解方案建议

### 图片输入不要直接拿来裸答
更推荐：

**图片 -> 识别对象/区域/按钮/文字 -> 生成检索 query -> 检索手册 -> 基于手册回答**

### 需要识别的信息

1. 产品类别
2. 关键部件 / 按钮 / 接口
3. 屏幕显示内容
4. 图片中的文本或图标
5. 用户关注区域

### 示例
用户问：
- “这个按钮是什么意思？”
- 同时上传空调遥控器图片

系统可先识别：
- 这是空调遥控器
- 图中可能是 `MODE` / `JET` / `ROOM TEMP` 按钮
- 然后去检索对应说明块

---

## 12. 多轮对话与记忆建议

### 最低要求
支持基于 `session_id` 的短期上下文管理。

### 需要记住的信息

1. 当前讨论的产品 / 型号
2. 用户上轮提到的部件
3. 最近一次已回答的步骤 / 现象
4. 当前问题是否为追问

### 示例
- 用户：这个灯一直闪是什么意思？
- 下一轮：那过热的话怎么办？

第二轮必须能理解“过热”指的是上一轮的指示灯状态，而不是新话题。

---

## 13. 幻觉抑制建议

### 必须具备的策略

1. **证据约束生成**
   - 只允许模型依据检索内容回答。

2. **低置信度保守回答**
   - 如果检索证据不足，避免强行给结论。

3. **输出自检**
   - 检查是否每个子问题都被回答。
   - 检查是否有超出证据的描述。

4. **禁止无依据扩展**
   - 不要自行补充手册没写的售后政策或参数。

### Prompt 原则
可以明确要求模型：
- 不要编造未在材料中出现的内容
- 多个子问题逐条作答
- 回答尽量引用手册术语
- 必要时指出“根据当前检索到的说明书内容”

---

## 14. 推荐项目目录

```text
multimodal_customer_agent/
├── app/
│   ├── main.py
│   ├── api/
│   │   └── chat.py
│   ├── schemas/
│   │   ├── request.py
│   │   └── response.py
│   ├── services/
│   │   ├── preprocess.py
│   │   ├── session_memory.py
│   │   ├── multimodal_understanding.py
│   │   ├── retriever.py
│   │   ├── reranker.py
│   │   ├── generator.py
│   │   └── guardrail.py
│   └── core/
│       ├── config.py
│       └── logger.py
├── data/
│   ├── raw/
│   ├── parsed/
│   ├── chunks/
│   ├── images/
│   └── index/
├── scripts/
│   ├── parse_manuals.py
│   ├── build_chunks.py
│   ├── build_image_mapping.py
│   ├── build_index.py
│   └── evaluate.py
├── tests/
├── docs/
├── requirements.txt
└── README.md
```

---

## 15. 推荐开发阶段划分

### 第 1 阶段：数据底座
目标：把资料处理成统一知识库。

要完成：
- 解析手册文本
- 解析图片 ID
- chunk 切分
- 图文绑定
- 建立标准 JSON/JSONL 文件

### 第 2 阶段：baseline
目标：能答题。

要完成：
- BM25 检索
- 向量检索
- top-k 召回
- LLM 基于证据生成回答
- 返回标准响应结构

### 第 3 阶段：多模态增强
目标：图片题不崩。

要完成：
- 图片理解
- query 改写
- 图文联合检索

### 第 4 阶段：多轮与守护
目标：更稳。

要完成：
- session 管理
- 子问题拆分
- 幻觉抑制
- 自检

### 第 5 阶段：评测与优化
目标：冲榜。

要完成：
- 离线评测集
- 错题分析
- chunk 优化
- rerank 优化
- prompt 优化

### 第 6 阶段：交付材料
目标：把系统设计分拿满。

要完成：
- API 说明
- 源码整理
- 技术文档
- 验证报告
- 演示材料

---

## 16. 现在最该优先做的事

### 优先级 1：先把知识库结构化
这是整个项目的底座。

### 优先级 2：先做一个可运行的 `/chat`
哪怕第一版不强，也必须尽快跑通。

### 优先级 3：先能返回“相对正确的文本 + 相关图片”
先把图文绑定跑通，再追求复杂能力。

### 优先级 4：尽快有离线评测能力
否则每次改动都不知道是在变好还是变差。

---

## 17. 容易踩坑的点

1. **只做纯文本向量库，不管图片**
   - 会丢高分点。

2. **只用一个大模型裸答**
   - 幻觉风险高，图文无法精确对齐。

3. **不做子问题拆分**
   - 一题多问会丢很多分。

4. **不做 rerank**
   - top-k 里会混入大量弱相关片段。

5. **全部内容塞进一个超长上下文**
   - 既贵又不稳。

6. **一直在 base 环境写代码**
   - 建议单独 conda 环境开发。

7. **太早微调**
   - baseline 未跑通前，不建议先微调。

8. **忽略系统设计材料**
   - 这部分占 30%，不能只顾着刷榜。

---

## 18. 推荐最小可用版本（MVP）

### MVP 目标
做到：
- 接收 `question`
- 可选接收 `images`
- 从知识库检索相关 chunk
- 生成基于证据的答案
- 支持 `session_id`
- 返回标准 JSON

### MVP 不要求
- 不要求先微调
- 不要求先做很复杂的多 Agent
- 不要求先做网页前端
- 不要求先做全流式输出

---

## 19. 推荐技术栈

### 后端
- Python
- FastAPI
- Uvicorn

### 检索
- FAISS 或其他向量库
- BM25（如 rank_bm25）
- reranker 模型

### 模型层
- 一个多模态理解模型
- 一个生成模型
- 一个 embedding 模型
- 可选：一个 reranker

### 数据处理
- json / jsonl
- pandas
- 正则 / 文本清洗

### 评测
- 自定义脚本
- case 分析表
- 自动保存预测结果

---

## 20. 给 Codex 的执行任务建议

可以按下面顺序让 Codex 开工：

### Task 1：搭项目目录
- 建立 app / scripts / data / docs 等目录
- 生成基础 FastAPI 项目骨架

### Task 2：写数据解析脚本
- 解析手册文本
- 识别 `<PIC>`
- 建立 chunk 与 image_id 映射
- 输出统一 JSONL

### Task 3：写检索模块
- BM25 检索
- 向量检索
- 混合召回

### Task 4：写生成模块
- 输入问题 + top-k chunks
- 输出客服风格答案
- 限制只基于证据回答

### Task 5：写 `/chat`
- 接收 question / images / session_id
- 返回标准格式

### Task 6：写基础评测脚本
- 读取题目
- 调用 `/chat`
- 保存结果
- 输出简单统计

### Task 7：后续增强
- 图片理解
- 子问题拆分
- reranker
- 守护与自检
- 多轮记忆

---

## 21. 一句话总结

这个比赛最优策略不是“先做一个炫酷智能体”，而是：

**先做一个稳定的多模态 RAG 客服系统，保证检索准、回答稳、图文对得上，再逐步增强多轮和守护能力。**

